// Include the entireety of the STL, a bad practice, but okay for Problem Solving.
#include <bits/stdc++.h>

// This file is a template to be used for Problem Solving in the ICPC, Codeforces, etc...

// Note that using is generally discouraged, but it's okay for problem solving.
using namespace std;
// In CPP it's better to inline and typedef than to use MACROS.
// Consider using a MACRO for |int x, *y;| typedeffing would initialize the second as a pointer.
// Typedeffing for faster typing.
typedef int64_t i64;
typedef int32_t i32;
typedef int16_t i16;
typedef int8_t i8;
typedef uint64_t ui64;
typedef uint32_t ui32;
typedef uint16_t ui16;
typedef uint8_t ui8;

int main() {
  // Unsyncs stdio and cin/cout, okay if you are only ever gonna use one of them.
  ios_base::sync_with_stdio(false);
  // Unties cin and cout, bad for human interaction, okay for computer interaction where output and input aren't necessarily correlated.
  cin.tie(NULL);


  
  // EXIT_SUCCESS -> 0
  return EXIT_SUCCESS;
}